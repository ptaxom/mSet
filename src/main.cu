#include "view/view.h"

int main(int argc, char **argv)
{

    // Render::Renderer<double>  r(std::complex<double>(0,0), 1200, 768, 255, 1.0);
    // Render::byte* x = r.render();
    init_window(argc, argv);
    return 0;
}